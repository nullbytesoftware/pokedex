import { CaptializePipe } from './captialize.pipe';

describe('CaptializePipe', () => {
  it('create an instance', () => {
    const pipe = new CaptializePipe();
    expect(pipe).toBeTruthy();
  });
});
